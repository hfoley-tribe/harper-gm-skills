---
date: {YYYY-MM-DD}
generated_at: {HH:MM ET}
sources: [calendar, slack, gmail, sybill, granola, project-os, vault]
---

# Daily Brief — {Weekday}, {Month D, YYYY}

## Today's shape

{One paragraph: what kind of day, what matters most, anything unusual.}

{If any source is ❌ or ⚠️, lead with a one-line heads-up here.}

## Meetings

{For each meeting, in time order. Skip the section if no meetings.}

- **{HH:MM} — {Title}** ({engagement code if any})
  - Attendees: {names}
  - Prep status: {ready | needs prep | blocked on X}
  - Link: {calendar / Granola / Sybill}
  - **Walk in knowing:** 1–3 bullets
  - **Walk out with:** 1–2 bullets

## Open threads I owe

{Things Harper owes a response on. Checkbox format. Sort by urgency.}

- [ ] {thing} — {who's waiting} — {since when} — {context link}

## Open threads waiting on others

{Things others owe Harper. Sort by impact.}

- {thing} — {who I'm waiting on} — {since when} — {what to do if no response by X}

## Engagement pulse

{For each active engagement with movement in last 24h. One line each. Skip engagements with no movement.}

- **GEV** ({Pattern B, active}): {what changed and what it means}
- **Elliott** ({Pattern B, discovery}): {what changed and what it means}
- **KKR** ({Pattern A, qualifying}): {what changed and what it means — never financials if tribe-shared}
- **HP** ({Pattern B, paused}): {only if movement; otherwise omit}
- **C&W** ({Pattern B, shadow}): {observation only — Trevor leads}

## Inbox highlights

{Only items needing action or awareness. Nothing routine. Skip the section if empty.}

- {item} — {sender / channel} — {action implied}

## Suggested priorities (in order)

{First, Harper's own priorities from yesterday's evening file (verbatim). Then, override candidates from live signal, marked clearly.}

1. {priority} — {why}
2. {priority} — {why}
3. {priority} — {why}

{Optional: "Override candidate: ..." — items live signal suggests should bump above Harper's stated priorities, with one-line reasoning.}

## Scenarios in play today

{Optional. Only surface entries from scenario-log.md whose triggers match today's signal. Skip if none.}

- {scenario name} — triggered by {what} — escalation: {what}

## Sources

- ✅ Calendar ({N} events, last 24h)
- ✅ Slack priority channels ({N} unread mentions / {N} threads with activity)
- ✅ Gmail VIP ({N} threads in window)
- ✅ Sybill ({N} calls yesterday)
- ✅ Granola ({N} notes since {since})
- ✅ Vault ({N} engagement READMEs read)
- {Pattern A entry — ✅ if Project OS path exists, ❌ otherwise}
- {Yesterday's brief — ✅ if existed, ❌ first run}

{Use ⚠️ for partial/stale sources, ❌ for failed pulls. Include the specific failure mode and remediation: "auth expired — re-auth", "MCP timeout", "path not found", etc.}
